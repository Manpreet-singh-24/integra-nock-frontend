const namespaces = 'user';

const Types = {
    LOGIN_REQUEST: `${namespaces}/LOGIN_REQUEST`,
    LOGIN: `${namespaces}/LOGIN`,
    DETAIL_REQUEST: `${namespaces}/DETAIL_REQUEST`,
    DETAIL_DATA : `${namespaces}/DETAIL_DATA`,
};

export default Types;
